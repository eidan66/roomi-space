import { Inter, Varela_Round as VarelaRound } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
export const varelaRound = VarelaRound({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-varela-round',
});
